services:
  - docker

jobs:
  include:
    - name: "Tests: Shared"
      script: make run-travis MODULE=shared
    - name: "Tests: Reader"
      script: make run-travis MODULE=reader
    - name: "Tests: Writer"
      script: make run-travis MODULE=writer USE_REDIS=1
